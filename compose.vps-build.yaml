# Fallback only when GitHub Actions / GHCR is unavailable.
# Prefer compose.yaml alone to pull GHCR images. Combine these files for a
# VPS-local build; the runtime data volume remains unchanged.
services:
  leave-system:
    image: dzgc-leave-system:local
    build:
      context: .
      dockerfile: Dockerfile
